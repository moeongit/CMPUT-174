# This program
name = input("What is the character's name? ") # Asks the user to input a character's name
age = int(input("What is the character's age? ")) # Asks the user to input a character's age
if age < 0:
    print("Invalid age.") # If the user prints an age below 0, the code will print out "Invalid Age".
else: